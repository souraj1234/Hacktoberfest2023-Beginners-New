#include <bits/stdc++.h>

#include <algorithm>

 

int findDuplicate(vector<int> &arr, int n){

    // Write your code here.

    sort(begin(arr),end(arr));

    for(int i=0;i<n;i++){

        if(arr[i] == arr[i+1]){

            return arr[i];

        }   

    }

}
